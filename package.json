{
  "name": "masofaviy2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lms.js && node --check scorm.js && node --check public/app.js && node --check public/sw.js",
    "test": "node --test tests/scorm.test.js",
    "prestart": "npm run build:media-client && npm run check",
    "build:media-client": "esbuild frontend/media-client.js --bundle --format=iife --global-name=MasofaviyMediaClientBundle --outfile=public/media-client.bundle.js --minify --target=es2020"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "socket.io": "^4.8.1",
    "exceljs": "^4.4.0",
    "fast-xml-parser": "^5.2.5",
    "jszip": "^3.10.1",
    "csv-parse": "^6.1.0",
    "ws": "^8.18.3",
    "mediasoup-client": "^3.18.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
